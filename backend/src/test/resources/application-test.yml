# Tests run against the containers declared in ContainersConfig: the connection details are injected
# by @ServiceConnection, so nothing is configured here. Flyway runs and Hibernate validates, exactly
# as in production, which is what makes a broken migration fail the build instead of the deploy.
spring:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
